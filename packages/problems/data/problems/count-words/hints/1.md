Use `wc` with the `-w` option to count the number of words in a file. For example, to count the number of words in a file named `example.txt`, you can use the following command:

```bash
wc -w example.txt
```
