You can use the `curl` command to make a request. Here is an example:

```
curl http://localhost:4000/most-recent-post
```

You can use the `-s` option to suppress the progress and only show the repsonse.
