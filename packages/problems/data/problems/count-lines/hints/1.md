Use `wc` with the `-l` option to count the number of lines in a file. For example, to count the number of lines in a file named `example.txt`, you can use the following command:

```bash
wc -l example.txt
```
